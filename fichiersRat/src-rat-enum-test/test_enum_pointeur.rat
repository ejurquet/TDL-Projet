enum Test {Test1, Test2, Test3};

pointeur{
    Test * x = (new Test);
    (*x) = Test2;
    Test z = Test1;
    Test *y = & z;
    print ((*y) = Test1);
    (* y) = (*x);
    Test ** w = (new Test*);
    (*w) = x;
    print ((* (* w)) = Test2);
}